#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * @c: character to check the case
 *
 * Return: Always return 0 (Success)
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);

}
